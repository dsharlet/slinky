common --enable_bzlmod
build --action_env=BAZEL_CXXOPTS="-std=c++17:-fstrict-aliasing:-Wall"
build --copt=-fdiagnostics-color=always
run --copt=-fdiagnostics-color=always
test --copt=-fdiagnostics-color=always

# Allow us to run with --config=asan
# (Note, this may assume that the system C++ compiler is Clang rather than GCC)
build:asan --strip=never
build:asan --copt -fsanitize=address
build:asan --copt -DADDRESS_SANITIZER
build:asan --copt -O1
build:asan --copt -g
build:asan --copt -fno-omit-frame-pointer
build:asan --linkopt -fsanitize=address
